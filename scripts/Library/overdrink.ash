//import "scripts/Library/consume.ash"



void main(string arguments)
{
	cli_execute("consume.ash overdrink " + arguments);
	//if (mpa.to_lower_case().contains_text("pvp")) __consume_for_pvp_fights_instead = true;
	//overdrink(mpa.to_int_silent());
}