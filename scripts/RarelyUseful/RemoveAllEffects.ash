void main()
{
	foreach e in $effects[]
	{
		if ($effects[everything looks red, everything looks blue, everything looks yellow] contains e) continue;
		if (e.have_effect() > 0 && e.have_effect() < 2147483647)
		{
			boolean ignore = cli_execute("uneffect " + e);
		}
	}
}